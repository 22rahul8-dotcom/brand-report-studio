# ── Core (required) ───────────────────────────────────────────────────────────
firecrawl-py>=1.0.0        # Phase 1: Brand scraping & branding extraction
anthropic>=0.30.0           # Phase 3: Content structuring & AI generation

# ── Image processing (recommended) ────────────────────────────────────────────
Pillow>=10.0.0              # Logo variant generation + k-means color extraction from images

# ── PDF export (optional — choose one) ────────────────────────────────────────
weasyprint>=60.0            # PDF export via HTML renderer (Linux/macOS, no Node required)
# playwright>=1.40.0        # Alternative PDF export via Chromium (needs: playwright install chromium)

# ── DOCX export (optional) ────────────────────────────────────────────────────
python-docx>=1.1.0          # Word document export for editable client handoff
